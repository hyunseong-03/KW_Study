배열을 만들어서 아래와 같이 저장한 후 출력하는 프로그램을 작성하시오. 
규칙은 첫 번째 행과 첫 번째 열은 모두 1로 초기화 하고
다음 항부터는 바로 위의 값과 바로 왼쪽의 값을 더한 것이다.


#include <stdio.h>

int main(void)
{
	int c[5][5] = { {1,1,1,1,1}, {0},{0},{0},{0}};
	int i = 0, j = 0, k = 0, l = 0;

	for (i = 1; i < 5; i++)
	{
		for (j = 0; j < 5; j++)
		{
			if (j == 0)
				c[i][j] = c[i - 1][j];
			else
				c[i][j] = c[i - 1][j] + c[i][j - 1];
		}
	}

	for (i = 0; i < 5; i++)
	{
		for (j = 0; j < 5; j++)
		{
			printf("%3d", c[i][j]);
		}
		printf("\n");
	}
	return 0;
}
