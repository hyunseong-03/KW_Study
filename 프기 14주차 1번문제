arrA[2][4] 배열은 1, 2, 3, 4, 5, 6, 7, 8로 초기화한다.
그리고 arrA 배열의 값을 읽어서 arrB[4][2] 배열로 값을 옮긴다.
그리고 arrB 배열을 아래 실행결과와 같이 출력한다.


#include <stdio.h>

int main(void)
{
	int arrA[2][4] = { 1,2,3,4,5,6,7,8 };
	int arrB[4][2];
	int i, j;

	for (i = 0; i < 2; i++)
	{
		for (j = 0; j < 4; j++)
		{
			arrB[j][i] = arrA[i][j];
		}
	}

	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 2; j++)
		{
			printf("%3d ", arrB[i][j]);
		}
		printf("\n");
	}
	printf("\n");


	return 0;
}
