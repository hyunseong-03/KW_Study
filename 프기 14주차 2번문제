아래 코드의 성적관리 프로그램을 완성하자. 과목은 국어, 영어, 수학 이렇게 3과목이고 학생은 4사람이다. 아래와 같이 미리 선언해 놓은 배열에 합계까지 저장한다.
계산 및 저장을 완료하고 이에 대한 전체 값을 출력한다.



#include <stdio.h>

void WriteSumRecord(int rec[][4]);
void ShowAllRecord(int rec[][4]);

int main(void) {
	int record[5][4] = { {40, 50, 60, 0}, {35, 45, 55, 0},
	{43, 53, 63, 0}, {62, 72, 82, 0}, {0} };
	WriteSumRecord(record);
	ShowAllRecord(record);
	return 0;
}

void WriteSumRecord(int rec[][4])
{
	int sum = 0;
	for (int i = 0; i < 4; i++)
	{
		sum = 0;

		for (int j = 0; j < 3; j++)
		{
			sum += rec[i][j];
			rec[i][3] = sum;
		}
	}

	for (int i = 0; i < 4; i++)
	{
		sum = 0;

		for (int j = 0; j < 4; j++)
		{
			sum += rec[j][i];
			rec[4][i] = sum;
		}
	}
}

void ShowAllRecord(int rec[][4])
{
	printf("학생\t국어\t영어\t수학\t합계\n");
	printf("학생1\t%d\t%d\t%d\t%d\n", rec[0][0], rec[0][1], rec[0][2], rec[0][3]);
	printf("학생2\t%d\t%d\t%d\t%d\n", rec[1][0], rec[1][1], rec[1][2], rec[1][3]);
	printf("학생3\t%d\t%d\t%d\t%d\n", rec[2][0], rec[2][1], rec[2][2], rec[2][3]);
	printf("학생4\t%d\t%d\t%d\t%d\n", rec[3][0], rec[3][1], rec[3][2], rec[3][3]);
	printf("합계\t%d\t%d\t%d\t%d\n", rec[4][0], rec[4][1], rec[4][2], rec[4][3]);
}
